# Without this, `COPY . .` overwrites the image's clean node_modules (copied
# from the deps stage) with the host's, and drags build caches into the image.
node_modules
.next
.git
.gitignore
*.md
!README.md
.env
.env.local
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
